{% extends 'base.html.twig' %}

{% block title %}Editar un articulo{% endblock %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}
    <h1 class="text-center">EDITANDO UN ARTÍCULO</h1>

<div class="container">
    {{ include('articulos/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex justify-content-evenly mb-3">
    <a class="btn-success btn-lg text-decoration-none p-3" href="{{ path('app_articulos_index') }}">VOLVER A LA LISTA</a>
    {{ include('articulos/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
